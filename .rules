# 🚨 REGLAS CRÍTICAS DEL PROYECTO - TOULOUSE DESIGN SYSTEM

## ❌ PROHIBIDO: FALLBACKS
- **NUNCA** usar fallbacks silenciosos que oculten errores
- **NUNCA** continuar la ejecución si hay errores críticos
- **NUNCA** generar archivos parciales o con errores
- **SIEMPRE** fallar rápido con errores claros y específicos
- **SIEMPRE** hacer rollback completo si algo falla

### ❌ Ejemplos PROHIBIDOS:
```javascript
// ❌ PROHIBIDO - Fallback silencioso
try {
    result = criticalOperation();
} catch (error) {
    console.warn('Error ignorado, continuando...');
    result = 'default_value'; // NUNCA HACER ESTO
}

// ❌ PROHIBIDO - Continuar con errores
if (!config.analytics.measurementId) {
    console.warn('GA4 ID faltante, deshabilitando analytics...');
    config.analytics.enabled = false; // NUNCA HACER ESTO
}
```

### ✅ Ejemplos CORRECTOS:
```javascript
// ✅ CORRECTO - Fail fast
if (!config.analytics.measurementId) {
    throw new Error('❌ FALTA GA4 Measurement ID en config.js → analytics.googleAnalytics.measurementId');
}

// ✅ CORRECTO - Rollback completo
if (error) {
    await rollbackCompleteGeneration();
    throw new Error(`❌ Generación falló: ${error.message}`);
}
```

## 🎯 FAIL-FAST PRINCIPLES

1. **DETECTAR errores temprano**
2. **FALLAR rápido** con mensajes claros
3. **ROLLBACK completo** - no dejar archivos parciales
4. **MOSTRAR ubicación exacta** del problema (archivo:línea)
5. **PROPONER solución específica** en el mensaje de error

## 📋 VALIDACIONES OBLIGATORIAS

### ⚠️ Antes de cualquier generación:
- [ ] **OBLIGATORIO**: Ejecutar `nvm use 24` SIEMPRE
- [ ] Verificar que `config.js` tenga todos los valores requeridos
- [ ] Verificar que `component-metadata.json` sea válido
- [ ] Verificar que Node.js sea versión 24+ (auto-validado)
- [ ] Verificar que PHP CLI esté disponible
- [ ] Verificar que Vite config sea válido

### 🔧 COMANDOS OBLIGATORIOS ANTES DE GENERAR:
```bash
nvm use 24              # SIEMPRE ejecutar PRIMERO
npm run wp:generate     # Después de nvm use 24
```

### ⚠️ Durante la generación:
- [ ] Validar cada archivo PHP inmediatamente después de crearlo
- [ ] Verificar que cada extensión se cargue correctamente
- [ ] Validar que cada manager tenga configuración válida
- [ ] Verificar sintaxis de templates generados

### ⚠️ Después de la generación:
- [ ] Validar sintaxis completa de PHP con `php -l`
- [ ] Verificar que todos los archivos requeridos existan
- [ ] Validar estructura de directorios
- [ ] Probar URLs si WordPress está corriendo

## 🚫 CASOS DE ERROR COMUNES

### 1. Configuración Faltante
```javascript
// ❌ ANTES (con fallback)
const measurementId = config.analytics?.googleAnalytics?.measurementId || 'G-DEFAULT';

// ✅ DESPUÉS (fail fast)
if (!config.analytics?.googleAnalytics?.measurementId) {
    throw new Error('❌ CONFIGURACIÓN FALTANTE: GA4 Measurement ID requerido en config.js');
}
```

### 2. Archivos Faltantes
```javascript
// ❌ ANTES (con fallback)
if (!fs.existsSync(metadataFile)) {
    console.warn('Metadata no encontrado, usando defaults...');
    return defaultMetadata;
}

// ✅ DESPUÉS (fail fast)
if (!fs.existsSync(metadataFile)) {
    throw new Error(`❌ ARCHIVO REQUERIDO: ${metadataFile} no encontrado`);
}
```

### 3. Versión de Node.js
```javascript
// ❌ ANTES (con fallback)
if (nodeVersion < 20) {
    console.warn('Node.js antiguo, algunas features pueden no funcionar...');
}

// ✅ DESPUÉS (fail fast)
if (nodeVersion < 24) {
    throw new Error('❌ VERSIÓN REQUERIDA: Node.js 24+ requerido. Actual: ' + nodeVersion);
}
```

### 4. Errores de Build
```javascript
// ❌ ANTES (con fallback)
try {
    await runViteBuild();
} catch (error) {
    console.warn('Build falló, usando assets básicos...');
    await generateBasicAssets();
}

// ✅ DESPUÉS (fail fast)
try {
    await runViteBuild();
} catch (error) {
    await rollbackGeneration();
    throw new Error(`❌ BUILD FALLÓ: ${error.message}\n💡 Ejecutar: npm run build para debug`);
}
```

## 🔧 COMANDOS DE VALIDACIÓN

### Antes de desarrollar:
```bash
nvm use 24                # SIEMPRE usar Node 24
npm run wp:validate-php   # Validar sintaxis PHP
npm run build             # Verificar que Vite funcione
```

### Durante desarrollo:
```bash
npm run wp:generate       # Debe fallar si hay problemas
npm run wp:test-urls      # Validar URLs en vivo
```

## 📝 MENSAJES DE ERROR REQUERIDOS

Todos los errores DEBEN incluir:

1. **❌ Símbolo de error claro**
2. **Descripción específica** del problema
3. **Ubicación exacta** (archivo:línea si aplica)
4. **💡 Solución propuesta** clara y actionable
5. **🧹 Rollback notification** si se ejecutó

### Ejemplo de mensaje CORRECTO:
```
❌ ANALYTICS MANAGER FALLÓ: GA4 Measurement ID no configurado
📍 Ubicación: scripts/wp-generator/core/config.js línea 81
💡 Solución: Cambiar 'G-XXXXXXXXXX' por tu GA4 ID real
🧹 Rollback ejecutado: archivos PHP limpiados
```

## 🛡️ ENFORCEMENT

### En código JavaScript:
```javascript
// Al inicio de cada manager
if (process.env.NODE_ENV !== 'test') {
    console.log('🚨 FAIL-FAST MODE: Errores causarán terminación inmediata');
}
```

### En scripts npm:
```json
{
  "scripts": {
    "wp:generate": "node -e \"process.env.FAIL_FAST=true\" && node scripts/config/generate-wp-templates.js"
  }
}
```

---

**🎯 RECUERDA: "Si hay errores que los procesos se trunquen, sino vamos a generar archivos que no sirven"**

## 🚨 REGLA CRÍTICA: NO MODIFICAR ARCHIVOS GENERADOS

### ❌ PROHIBIDO:
- **NUNCA** modificar archivos en `wordpress-output/` directamente
- **NUNCA** editar `.php` generados manualmente
- **NUNCA** hacer cambios temporales en archivos de salida

### ✅ CORRECTO:
- **SIEMPRE** modificar los generadores en `scripts/wp-generator/`
- **SIEMPRE** regenerar el tema completo después de cambios
- **SIEMPRE** probar con `npm run wp:generate` después de modificar generadores

### 📍 Ubicaciones de generadores:
- `scripts/wp-generator/managers/analytics-manager.js` → `inc/analytics-manager.php`
- `scripts/wp-generator/templates/functions-template.js` → `functions.php`
- `scripts/wp-generator/templates/php-components.js` → `components/*.php`
- `scripts/wp-generator/managers/seo-manager.js` → `inc/seo-manager.php`

### 🔄 Flujo CORRECTO para fixes:
1. Identificar problema en archivo generado
2. Localizar el generador correspondiente
3. Modificar el generador
4. Ejecutar `npm run wp:generate`
5. Validar con `npm run wp:test-urls`


🛡️ REGLAS DE WORDPRESS DEVELOPMENT

✅ Escapado y Sanitización Obligatoria
	•	OBLIGATORIO escapar salidas dinámicas:
	•	esc_html() → texto
	•	esc_url() → urls
	•	esc_attr() → atributos HTML
	•	wp_kses_post() → contenido rico
	•	OBLIGATORIO sanitizar inputs con sanitize_text_field(), sanitize_email(), etc.
	•	❌ PROHIBIDO imprimir datos de WP sin escape.

🌍 Internacionalización (i18n)
	•	OBLIGATORIO envolver textos duros con __(), _e(), esc_html__(), etc.
	•	OBLIGATORIO usar un text-domain consistente.
	•	❌ PROHIBIDO hardcodear strings sin traducción.

📂 Organización y Estructura
	•	OBLIGATORIO seguir jerarquía de plantillas de WP (page.php, single.php, etc.).
	•	OBLIGATORIO usar get_template_part() en lugar de require.
	•	OBLIGATORIO mantener queries en inc/ y presentación en plantillas.
	•	❌ PROHIBIDO mezclar queries complejas en plantillas.

⚙️ Queries y Loop
	•	OBLIGATORIO usar WP_Query si hay paginación o control avanzado.
	•	OBLIGATORIO llamar wp_reset_postdata() tras un loop secundario.
	•	❌ PROHIBIDO queries sin parámetros seguros.

🔒 Seguridad
	•	OBLIGATORIO wp_nonce_field() y check_admin_referer() en formularios.
	•	OBLIGATORIO validar todos los $_POST, $_GET, $_REQUEST.
	•	❌ PROHIBIDO usar valores de usuario sin validar.

🎨 Encolado de Scripts y Estilos
	•	OBLIGATORIO wp_enqueue_script y wp_enqueue_style.
	•	❌ PROHIBIDO inyectar <script> o <link> en plantillas.

📢 Errores y Debug
	•	OBLIGATORIO usar wp_die() en errores críticos.
	•	OBLIGATORIO deshabilitar WP_DEBUG_DISPLAY en producción.

⸻

🛡️ REGLAS ADICIONALES PARA ACF
	•	❌ PROHIBIDO usar the_field() → siempre usar get_field() + escape.
	•	✅ OBLIGATORIO validar campos requeridos. Fallar rápido si no existen.
	•	✅ OBLIGATORIO escapar según tipo de campo:
	•	Texto → esc_html()
	•	URL → esc_url()
	•	WYSIWYG → wp_kses_post()
	•	✅ OBLIGATORIO sanitizar en acf/update_value antes de guardar.
	•	✅ OBLIGATORIO documentar en component-metadata.json qué campos ACF usa cada componente.
	•	❌ PROHIBIDO hardcodear valores de ACF como fallback.

## 🚨 REGLA CRÍTICA: CODIFICACIÓN ROBUSTA Y ESCALABLE

### ❌ PROHIBIDO: CÓDIGO FRÁGIL Y FIXES PUNTUALES
- **NUNCA** implementar fixes que solo arreglen síntomas específicos
- **NUNCA** crear soluciones que solo funcionen para casos particulares
- **NUNCA** usar bandas temporales (flags, hacks) sin solucionar la causa raíz
- **SIEMPRE** analizar la causa raíz de los problemas
- **SIEMPRE** implementar soluciones comprehensivas que manejen todos los contextos

### ✅ OBLIGATORIO: ARQUITECTURA ROBUSTA
- **OBLIGATORIO** implementar sistemas de contexto completos para todas las conversiones
- **OBLIGATORIO** manejar todos los scopes (variables, loops, parámetros, funciones)
- **OBLIGATORIO** validar consistencia de variables en todos los contextos
- **OBLIGATORIO** diseñar sistemas que escalen para casos futuros no previstos

### 📋 VALIDACIONES DE ROBUSTEZ OBLIGATORIAS

#### Para Babel AST Converter:
- [ ] **OBLIGATORIO**: Sistema de contexto comprehensivo que maneje:
  - Variables disponibles en cada scope
  - Contexto de loops (foreach, while, for)
  - Contexto de funciones y parámetros
  - Contexto de condicionales anidadas
- [ ] **OBLIGATORIO**: Validación de variables antes de uso
- [ ] **OBLIGATORIO**: Mapeo correcto de variables entre contextos (JS → PHP)
- [ ] **OBLIGATORIO**: Fail-fast si variable no definida en contexto

#### Para Generadores WordPress:
- [ ] **OBLIGATORIO**: Cada generador debe validar completamente sus inputs
- [ ] **OBLIGATORIO**: Sistemas de rollback granulares por módulo
- [ ] **OBLIGATORIO**: Contexto de dependencias entre managers
- [ ] **OBLIGATORIO**: Validación cruzada entre archivos generados

### 🎯 PRINCIPIOS DE DISEÑO ROBUSTO

1. **CONTEXTO COMPLETO**: Cada sistema debe manejar todos los contextos posibles
2. **VALIDACIÓN EXHAUSTIVA**: Validar cada variable, parámetro, dependencia
3. **FAIL-FAST INTELIGENTE**: Fallar temprano con información de contexto
4. **ESCALABILIDAD INHERENTE**: Diseñar para casos no previstos

### ❌ Ejemplos PROHIBIDOS (FIXES PUNTUALES):
```javascript
// ❌ PROHIBIDO - Fix puntual solo para PHP tags
if (this.inPHPContext) {
    // Solo arregla síntoma, no causa raíz
}

// ❌ PROHIBIDO - Hardcodear variables específicas
if (variableName === 'showSuggestions') {
    // Solo funciona para este caso específico
}
```

### ✅ Ejemplos CORRECTOS (SISTEMAS ROBUSTOS):
```javascript
// ✅ CORRECTO - Sistema de contexto comprehensivo
class ContextTracker {
    constructor() {
        this.scopes = [];
        this.variables = new Map();
        this.loops = new Map();
    }

    validateVariable(name, context) {
        if (!this.isVariableAvailable(name, context)) {
            throw new Error(`❌ VARIABLE NO DEFINIDA: ${name} en contexto ${context}`);
        }
    }
}

// ✅ CORRECTO - Mapeo completo de contextos
convertToPhp(node, context) {
    const phpContext = this.contextMapper.mapContext(node, context);
    return this.generatePhpWithContext(node, phpContext);
}
```

**🎯 OBJETIVO**: Toda codificación debe ser robusta, escalable y manejar contextos completos - sin excepciones ni fixes puntuales.